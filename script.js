/* =====================================================
   Data Manager App - Your Unit 5 Project
   =====================================================

   Your mission: Build a mini database app using arrays,
   objects, and functions. Pick a topic you like, then
   manage it with code!

   Topic ideas (pick one, or make up your own):
   - Library (books with title, author, year)
   - Students (name, grade, GPA)
   - Pets (name, species, age)
   - Games (title, platform, rating)
   - Songs (title, artist, duration)
   - Movies (title, genre, year)

   Requirements:
   - Array of AT LEAST 4 objects, each with 3+ properties
   - AT LEAST 3 custom functions (1 must return data)
   - 2 different types of loops (for / for...of / forEach)
   - Use .push() to add new entries
   - Use ONE of: .find(), .filter(), or .sort()
   - Lots of console.log() so you can see what's happening
   ===================================================== */


// =====================================================
// STEP 1: Create your data array
// =====================================================
// Hint: Make an array with AT LEAST 4 objects. Each object
// needs AT LEAST 3 properties.
//
// Example structure (but pick your own topic!):
// let books = [
//   { title: "...", author: "...", year: ... },
//   { title: "...", author: "...", year: ... },
//   ...
// ];
//
// Write your code here:




// =====================================================
// STEP 2: Write a function to DISPLAY all items
// =====================================================
// Hint: Make a function that takes the array as a parameter
// and uses a for...of loop to console.log each item in a
// nice format using template literals.
//
// Something like:
// function showAll(list) {
//   for (let item of list) {
//     console.log(`${item.title} by ${item.author}`);
//   }
// }
//
// Write your code here:




// =====================================================
// STEP 3: Write a function to ADD a new item
// =====================================================
// Hint: Make a function that takes the new item's properties
// as parameters, then pushes a new object to the array.
//
// Something like:
// function addItem(title, author, year) {
//   list.push({ title: title, author: author, year: year });
// }
//
// Remember: your function should match YOUR topic's properties,
// not the example above.
// Write your code here:




// =====================================================
// STEP 4: Write a function that RETURNS data
// =====================================================
// Hint: This function should FIND something in the array and
// return it. Use .find() or a for...of loop.
//
// Example ideas:
// - Find an item by name/title
// - Find the oldest or highest-rated
// - Find the first item that matches a condition
//
// This function MUST use the return keyword.
// Write your code here:




// =====================================================
// STEP 5: Use .find(), .filter(), or .sort()
// =====================================================
// Hint: Pick ONE of these array methods and use it to
// organize or search your data.
//
// .find()   -> returns the FIRST item that matches
// .filter() -> returns ALL items that match
// .sort()   -> sorts the array
//
// Examples:
//   let young = list.filter(item => item.age < 5);
//   let sorted = list.slice().sort((a, b) => a.year - b.year);
//
// Write your code here:




// =====================================================
// STEP 6: Call your functions to test everything
// =====================================================
// Hint: Run each of your functions and console.log the results.
// This is where you see your app in action!

// 1. Show all items first
// 2. Add a new item
// 3. Show all items again (see the new one?)
// 4. Search/filter/sort and show the result

// Write your code here:




// =====================================================
// BONUS CHALLENGES (pick at least 2)
// =====================================================
//
// BONUS 1: Delete an item
// Add a function deleteItem(name) that removes an item
// by a unique property (like name or title). Hint: use
// .findIndex() and .splice(), or .filter().
//
//
// BONUS 2: Update an item
// Add a function updateItem(name, property, newValue) that
// finds an item and changes one of its properties.
//
//
// BONUS 3: Summary / stats function
// Write a function that analyzes the array. Like "average
// age of pets" or "how many books are from the 1990s."
//
//
// BONUS 4: Display on the page
// Instead of just console.log, update the DOM with your
// data. Hint: document.getElementById, or create elements.



// =====================================================
// DONE! Save and open index.html. Your output will be in
// the console (F12). Try running your add/find/filter
// functions and watch the data change!
// =====================================================
